### Start venv
```
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

```

### Start notebook
Inicie o notebook após a instalação e início da venv.
```
python -m notebook
```